

// document.addEventListener("DOMContentLoaded", function (event) {
//   const showNavbar = (toggleId, navId, bodyId, headerId) => {
//     const toggle = document.getElementById(toggleId),
//       nav = document.getElementById(navId),
//       bodypd = document.getElementById(bodyId),
//       headerpd = document.getElementById(headerId);

//     // Validate that all variables exist
//     if (toggle && nav && bodypd && headerpd) {
//       toggle.addEventListener("click", () => {
//         // show navbar
//         nav.classList.toggle("show");
//         // change icon
//         toggle.classList.toggle("bx-x");
//         // add padding to body
//         bodypd.classList.toggle("body-pd");
//         // add padding to header
//         headerpd.classList.toggle("body-pd");
//       });
//     }
//   };

//   showNavbar("header-toggle", "nav-bar", "body-pd", "header");
// });
//   /*===== LINK ACTIVE =====*/
// // Attend que le DOM soit entièrement chargé avant d'exécuter le script
// document.addEventListener("DOMContentLoaded", function() {
//   // Sélectionne tous les éléments avec la classe "nav_link"
//   const linkColor = document.querySelectorAll(".nav_link");
// });})
// //   Définit une fonction pour changer la couleur du lien actif
//   function colorLink() {
//     // Vérifie si des éléments avec la classe "nav_link" ont été trouvés
//     if (linkColor) {
//       // Parcourt tous les liens et retire la classe "active" pour les désactiver
//       linkColor.forEach((l) => l.classList.remove("active"));
//       // Ajoute la classe "active" uniquement sur le lien cliqué (this)
//       this.classList.add("active");
//     }
//   }

// //   // Associe l'événement "click" à chaque lien pour déclencher la fonction colorLink
//   linkColor.forEach((l) => l.addEventListener("click", colorLink));


// //   linkColor.forEach((l) => l.addEventListener("click", colorLink));

// //   // Your code to run since DOM is loaded and ready
// // });
// // // Initialization for ES Users
